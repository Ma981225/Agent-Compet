<<<<<<< HEAD
# 租房需求匹配智能体

一个基于自然语言处理的智能租房推荐系统，支持多平台房源搜索、多维度评价和智能推荐。

## 功能特性

1. **自然语言需求理解**：从用户自然语言输入中提取租房需求，支持主动追问
2. **多平台房源聚合**：支持链家、安居客、58同城等多个平台的房源数据获取
3. **智能筛选核验**：根据需求自动筛选房源，并进行信息完整性核验
4. **多维度评价**：从通勤距离、租金性价比、生活配套等维度分析房源
5. **智能推荐**：输出最多5套高匹配候选房源，包含详细评价

## 支持的用例

1. **聊天类**：基本对话能力
2. **单轮对话简单任务**：
   - 单条件查询：如"查询朝阳区房源"
   - 复杂条件查询：如"查询海淀+两居+近地铁+电梯+民水民电+预算5000+60平以上的房源"
3. **多轮对话复杂任务**：
   - 从吐槽当前房子到找到新房子再到租房（5轮）
   - 链家安居客58同城多家交叉筛选、比价、决策（6轮）
   - 从电梯、地铁、装修、朝向、采光、入住反复筛选（7轮）

## 项目结构

```
.
├── agent.py                 # 主程序入口
├── config.py                # 配置文件
├── models/                  # 数据模型
│   ├── house.py            # 房源信息模型
│   └── requirement.py      # 租房需求模型
├── services/                # 服务模块
│   ├── requirement_extractor.py  # 需求提取服务
│   ├── house_fetcher.py    # 房源获取服务
│   ├── house_filter.py     # 房源筛选服务
│   └── house_evaluator.py  # 房源评价服务
└── utils/                   # 工具模块
    └── logger.py           # 日志工具
```

## 安装依赖

```bash
pip install -r requirements.txt
```

## 配置

创建 `.env` 文件，配置以下参数：

```env
API_BASE_URL=http://localhost:8000
OPENAI_API_KEY=your_api_key
OPENAI_BASE_URL=https://api.openai.com/v1
MODEL_NAME=gpt-3.5-turbo
MAX_TOKENS=2000
TEMPERATURE=0.7
```

## 使用方法

### 命令行运行

```bash
python agent.py
```

### 代码调用

```python
from agent import RentalHouseAgent

agent = RentalHouseAgent()
reply = agent.process_user_input("我想在朝阳区租一套两居室，预算5000元")
print(reply)
```

## 使用示例

### 示例1：简单查询

```
您：我想在朝阳区租房
助手：好的，我为您推荐朝阳区房源。您对租金、户型、面积有什么要求吗？
```

### 示例2：复杂条件查询

```
您：查询海淀+两居+近地铁+电梯+民水民电+预算5000+60平以上的房源
助手：为您找到 5 套高匹配房源：
【房源 1】
标题：海淀区精装两居室
位置：海淀区 中关村大街
租金：4800元/月
...
```

### 示例3：多轮对话

```
您：现在的房子太吵了，想换个安静的地方
助手：理解您的困扰。请问您想换到哪个区域？预算大概是多少？

您：朝阳区，预算6000左右
助手：好的，朝阳区预算6000元。您对户型、面积有什么要求吗？

您：两居室，最好近地铁
助手：为您找到 5 套高匹配房源...
```

## 评分优化

系统已针对比赛评分规则进行优化：

1. **Token消耗优化**：
   - 精简系统提示词
   - 限制对话历史长度（仅保留最近2轮）
   - 实现房源数据缓存机制
   - 批量处理房源评价

2. **功能完整性**：
   - 支持所有要求的用例场景
   - 多平台数据聚合
   - 多维度评价分析

## 注意事项

1. 确保API服务正常运行
2. 需要配置有效的OpenAI API密钥
3. 根据实际API接口调整 `house_fetcher.py` 中的接口调用

## 许可证

MIT License
=======
# Agent-Compet
>>>>>>> d75395c88091241340c341714ac63816361abe47
